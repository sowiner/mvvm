import 'package:flutter_test/flutter_test.dart';

void main() {
  group("app.dart tests", () {
    test("single app test", () {
      // final s1 = App();
      // final s2 = App();
      // expect(s1, s2);
    });
  });
}
